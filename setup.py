from setuptools import setup, find_packages

setup(
    name="VibrantImageColorization",
    version="0.0",
    description="A library for vibrant image colorization based on a custom loss function (in development)!",
    author="Jordan Sinclair",
    packages=find_packages()
)